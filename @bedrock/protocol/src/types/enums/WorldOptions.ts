export enum Difficulty {
	Peaceful,
	Easy,
	Normal,
	Hard,
}
export enum GameMode {
	Survival,
	Creative,
	Adventure,
	SurvivalSpectator,
	CreativeSpectator,
	Fallback,
	Spectator,
}
export enum GameRuleType {
	Bool = 1,
	Int = 2,
	Float = 3,
}
